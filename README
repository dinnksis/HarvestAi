запуск через докер

docker-compose up --build -d


проверка что все 3 контейнера есть(постгре, фронт и бэк)
docker-compose ps 

Start-Process "http://localhost:3000" 
если херня, подожите минуту и потом запускайте


логи если что-то не то:
docker-compose logs --tail=20
docker-compose logs frontend/backend --tail=20

если не работает или заново надо собрать:
удаление старых контейнеров и сетей
docker-compose down -v
docker network prune -f


ml:
в real_model.py

class RealFertilizerModel:
    ├── __init__(self, model_path=None)   
    └── predict(self, field_data)  + другие
требования к предикту(вход):
{
    "boundary": {  # ГРАНИЦЫ ПОЛЯ 
        "type": "Polygon",
        "coordinates": [[
            [долгота1, широта1], 
            [долгота2, широта2],
            [долгота3, широта3],
            [долгота1, широта1]   
        ]]
    }
}


требования к предикту(выход):
#СПИСОК из 900 ТОЧЕК
[
    # ТОЧКА 1
    {
        "x": 37.317600,      # долгота (float, 6 знаков после запятой)
        "y": 55.155800,      # широта (float, 6 знаков после запятой)
        "value": 45.3,       # удобрения кг/га (float, от 0 до 100)
        # "ndvi": 0.45       # опционально (если есть)
    },
    
    # ТОЧКА 2
    {
        "x": 37.317800,
        "y": 55.155900,
        "value": 38.7
    },
    
    # ... И ТАК 900 ТОЧЕК
]

названия ключей:          
{"x": ..., "y": ...}     
{"value": ...}            

диапазон значений:
"value": 45.3
- Допустимый диапазон: 0.0 - 100.0
- Тип: float (можно int, но лучше float)
- Единицы: килограммы удобрений на гектар (кг/га)
